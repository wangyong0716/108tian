<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:custom="http://schemas.android.com/apk/res-auto"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:background="@color/white"
             android:orientation="vertical">
    <com.ksider.mobile.android.scrollListView.OverScrollPullToZoomListView
            android:id="@+id/listview"
            android:layout_width="match_parent"
            android:layout_height="fill_parent"
            android:dividerHeight="@dimen/margin_standard"
            android:listSelector="@color/transparent"
            custom:listHeadView="@layout/choiceness_detail_header"/>
    <include layout="@layout/header_share_toolbar"/>
</FrameLayout>